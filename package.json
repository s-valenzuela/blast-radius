{
  "name": "blast-radius",
  "version": "0.1.0",
  "private": true,
  "description": "Visualize service dependency blast radius",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p jsconfig.json"
  },
  "devDependencies": {
    "js-yaml": "^4.1.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
